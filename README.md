# mysql-auto-backup
This PHP script automates MySQL database backups, compresses them into a zip file, and sends the backup to specified Telegram users. It can be run via a cron job for periodic backups at specified intervals. Simple to configure, secure, and perfect for managing regular database backups with Telegram notifications.
